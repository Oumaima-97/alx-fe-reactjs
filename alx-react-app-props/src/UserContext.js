// src/UserContext.js
import { createContext } from "react";

const UserContext = createContext(null);  // Correct context initialization

export default UserContext;  // Exporting the context so it can be used in other components
